import React from "react";
import Navigation from "./src/navigation";

const App = () => {
  return (
    // <Provider store={configureStore()}>
      <Navigation />
    // </Provider>
  );
};

export default App;
